{
  "type": "module",
  "scripts": {
    "test": "vitest",
    "build": "rm -rf ./dist && tsc",
    "start": "npm run build && node dist/server.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.17",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  },
  "dependencies": {
    "@apollo/server": "^4.12.0",
    "graphql": "^16.11.0"
  }
}
